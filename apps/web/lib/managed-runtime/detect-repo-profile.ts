// Stub — implementation pending (RED state)
// detectRepoProfile maps repository file markers to a managed-runtime profile id.

export function detectRepoProfile(
  _files: { path: string }[] | string[],
): string | null {
  // Not implemented yet — tests will fail
  return null;
}

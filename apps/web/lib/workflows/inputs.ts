// Stub — intentionally empty to produce behavioral test failures (RED phase)
export type WorkflowInputSchemaError = never;
export type WorkflowInputField = never;
export type WorkflowInputSchema = never;
export function parseWorkflowInputSchema(_def: unknown): never {
  throw new Error("not implemented");
}

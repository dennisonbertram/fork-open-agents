"use client";

// Stub — implementation pending
export function useRepoDefaults(_params: {
  enabled: boolean;
  repoOwner: string;
  repoName: string;
}) {
  return {
    defaults: undefined,
    loading: false,
    error: null,
  };
}

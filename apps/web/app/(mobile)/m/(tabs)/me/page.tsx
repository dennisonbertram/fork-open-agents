import { MobileMeScreen } from "@/components/mobile/me/mobile-me-screen";

/**
 * /m/me — Mobile Me / Profile screen.
 * Renders user profile and theme toggle via MobileMeScreen (client component).
 */
export default function MobileMePage() {
  return <MobileMeScreen />;
}

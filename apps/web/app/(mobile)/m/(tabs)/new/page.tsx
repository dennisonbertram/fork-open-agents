import { MobileNewSessionScreen } from "@/components/mobile/new/mobile-new-session-screen";

/**
 * /m/new — Mobile New Session.
 * Renders the create-session form via MobileNewSessionScreen (client component).
 */
export default function MobileNewPage() {
  return <MobileNewSessionScreen />;
}

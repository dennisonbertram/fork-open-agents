import { MobileActivityScreen } from "@/components/mobile/activity/mobile-activity-screen";

/**
 * /m — Mobile Activity home.
 * Renders the real-data activity list via MobileActivityScreen (client component).
 */
export default function MobileActivityPage() {
  return <MobileActivityScreen />;
}

/**
 * useLoopRunPolling — pure interval calculator (STUB)
 * Returns 0 for all statuses (will fail tests)
 */

export function computeLoopRunRefreshInterval(
  _status: string | undefined,
): number {
  return 0; // STUB: always returns 0 — tests will fail
}

{
  "name": "poc-4c-snapshot-vms",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "POC 4c: persistent / snapshottable VMs — prove the hibernate/resume lifecycle state machine and the filesystem+service+git restore mechanism against a local fake SnapshotProvider, mirroring the real Vercel Sandbox snapshot()/getOrCreate model and apps/web lifecycle.ts states.",
  "scripts": {
    "eval": "bun run src/eval.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}

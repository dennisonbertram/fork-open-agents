{
  "name": "poc-3b-sandbox-local-handoff",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "POC 3b: sandbox<->local state handoff — export branch + commit graph + uncommitted staged/unstaged/untracked working state as a portable git bundle and re-hydrate it byte-exactly on a fresh clone (both directions)",
  "scripts": {
    "eval": "bun run src/eval.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}

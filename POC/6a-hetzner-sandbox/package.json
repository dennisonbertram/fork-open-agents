{
  "name": "poc-6a-hetzner-sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted Hetzner-VM-backed Sandbox provider POC (Docker eval stand-in + real HcloudDriver).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:conformance": "tsc -p tsconfig.conformance.json --noEmit",
    "test:eval": "bun test test/eval.test.ts",
    "test:hcloud": "bun test test/hcloud-driver.test.ts",
    "test": "bun test",
    "eval": "bun run scripts/run-eval.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "20.14.0",
    "typescript": "5.6.3"
  }
}

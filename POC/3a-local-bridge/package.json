{
  "name": "poc-3a-local-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "POC 3a: local bridge daemon/CLI — diff apply + guarded local-exec handoff between cloud agent and the user's machine",
  "scripts": {
    "eval": "bun run src/eval.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.0"
  }
}

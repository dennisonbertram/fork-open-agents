# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo
.turbo
.swc

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
.devtools
.vercel
.env*.local

# ide
.claude
.claire

# open-agents dev server artifacts
.open-agents-dev-server-state.json
.open-agents-dev-server-*.pid

# any other dotenv file — apps/web/.gitignore already does this; the root
# did not, so a stray .env.production or .env.backup was committable
.env*

# Generated kanban board (source of truth is kanban.json)
.kanban/
apps/web/storybook-static/
